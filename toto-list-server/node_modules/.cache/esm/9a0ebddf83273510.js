let Category,Joi;_832‍.x([["input",()=>input]]);_832‍.w("../../models/category",[["default",["Category"],function(v){Category=v}]]);_832‍.w("@hapi/joi",[["default",["Joi"],function(v){Joi=v}]]);


       const input = async (ctx) => {
  const schema = Joi.object().keys({
    title: Joi.string().required(),
    color: Joi.string().required(),
  });
  const result = schema.validate(ctx.request.body);
  if (result.error) {
    ctx.status = 400;
    ctx.body = result.error;
    return;
  }
  const { title, color } = ctx.request.body;
  const category = new Category({
    title,
    color,
  });
  try {
    await category.save();
    ctx.body = category;
  } catch (e) {
    ctx.throw(500, e);
  }
};
